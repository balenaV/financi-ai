<x-app-layout><x-slot name="title">Editar orçamento</x-slot><x-page-header title="Editar orçamento" />
<form method="POST" action="{{ route('budgets.update',$budget) }}" class="surface mx-auto max-w-2xl p-6">@csrf @method('PUT')<div class="grid gap-5 sm:grid-cols-2"><x-form.select label="Categoria" name="category_id" required>@foreach($categories as $category)<option value="{{ $category->id }}" @selected(old('category_id',$budget->category_id)==$category->id)>{{ $category->name }}</option>@endforeach</x-form.select><x-form.input label="Limite" name="limit_amount" data-money-input :value="$budget->limit_amount" required /><x-form.select label="Mês" name="month" required>@foreach(range(1,12) as $m)<option value="{{ $m }}" @selected(old('month',$budget->month)==$m)>{{ \Carbon\Carbon::create(null,$m)->translatedFormat('F') }}</option>@endforeach</x-form.select><x-form.input label="Ano" name="year" type="number" :value="$budget->year" required /><div class="sm:col-span-2"><x-form.textarea label="Observações" name="notes" :value="$budget->notes" /></div></div><div class="mt-7 flex justify-end gap-2"><a href="{{ route('budgets.index') }}" class="btn-secondary">Cancelar</a><x-button type="submit">Salvar</x-button></div></form>
</x-app-layout>
