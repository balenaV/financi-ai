<x-app-layout>
    <x-slot name="title">{{ $debt->name }}</x-slot>
    <x-page-header :title="$debt->name" :description="'Credor: '.$debt->creditor"><a href="{{ route('debts.edit', $debt) }}" class="btn-secondary">Editar</a></x-page-header>
    <div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-4"><x-financial-card label="Valor total" :value="$summary['total']" /><x-financial-card label="Total pago" :value="$summary['paid']" tone="positive" /><x-financial-card label="Saldo devedor" :value="$summary['remaining']" tone="negative" /><article class="surface p-5"><p class="text-sm text-slate-500">Progresso</p><p class="mt-2 text-2xl font-bold">{{ $summary['percentage'] }}%</p><div class="mt-3"><x-progress :value="$summary['percentage']" tone="success" /></div></article></div>
    <div class="surface mt-6 overflow-hidden"><div class="border-b border-slate-200 px-5 py-4"><h2 class="font-bold">Parcelas</h2></div><div class="overflow-x-auto"><table class="w-full min-w-[760px] text-left text-sm"><thead class="bg-slate-50 text-xs uppercase text-slate-500"><tr><th class="px-5 py-3">Parcela</th><th class="px-5 py-3">Vencimento</th><th class="px-5 py-3">Status</th><th class="px-5 py-3 text-right">Valor</th><th class="px-5 py-3">Pagamento</th></tr></thead><tbody class="divide-y divide-slate-100">
    @foreach($debt->installments as $installment)<tr><td class="px-5 py-4 font-semibold">{{ $installment->number }}/{{ $debt->installment_count }}</td><td class="px-5 py-4">{{ $installment->due_date->format('d/m/Y') }}</td><td class="px-5 py-4"><x-badge :tone="$installment->status->value">{{ $installment->status->label() }}</x-badge></td><td class="px-5 py-4 text-right font-bold"><x-money :value="$installment->amount" /></td><td class="px-5 py-4">@if($installment->status->value === 'paid')<span class="text-sm text-accent-800">Pago em {{ $installment->paid_at->format('d/m/Y') }}</span>@elseif($debt->status->value !== 'cancelled')<form method="POST" action="{{ route('debts.installments.pay', $installment) }}" class="flex items-center gap-2" data-confirm="Confirmar pagamento e criar a despesa?">@csrf<select name="account_id" class="form-control !mt-0 !min-w-40" required><option value="">Conta</option>@foreach($accounts as $account)<option value="{{ $account->id }}">{{ $account->name }}</option>@endforeach</select><input type="date" name="paid_at" value="{{ today()->format('Y-m-d') }}" class="form-control !mt-0 !w-auto"><button class="btn-primary !min-h-9">Pagar</button></form>@endif</td></tr>@endforeach
    </tbody></table></div></div>
</x-app-layout>
