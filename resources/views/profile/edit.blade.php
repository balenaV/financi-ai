<x-app-layout>
    <x-slot name="title">Perfil e segurança</x-slot><x-page-header title="Perfil e segurança" description="Atualize seus dados de acesso."><a href="{{ route('settings.edit') }}" class="btn-secondary">Preferências</a></x-page-header>
    <div class="grid gap-6 xl:grid-cols-2">
        <form method="POST" action="{{ route('profile.update') }}" class="surface p-6">@csrf @method('PATCH')<h2 class="font-bold">Informações pessoais</h2><div class="mt-5 space-y-4"><x-form.input label="Nome" name="name" :value="$user->name" required /><x-form.input label="E-mail" name="email" type="email" :value="$user->email" required /><x-button type="submit">Salvar perfil</x-button></div></form>
        <form method="POST" action="{{ route('password.update') }}" class="surface p-6">@csrf @method('PUT')<h2 class="font-bold">Alterar senha</h2><div class="mt-5 space-y-4"><x-form.input label="Senha atual" name="current_password" type="password" required /><x-form.input label="Nova senha" name="password" type="password" required /><x-form.input label="Confirmar nova senha" name="password_confirmation" type="password" required /><x-button type="submit">Atualizar senha</x-button></div></form>
    </div>
    <form method="POST" action="{{ route('profile.destroy') }}" class="mt-6 rounded-2xl border border-red-200 bg-red-50 p-6" data-confirm="Excluir sua conta e todos os dados? Esta ação não pode ser desfeita.">@csrf @method('DELETE')<h2 class="font-bold text-red-900">Excluir conta</h2><p class="mt-1 text-sm text-red-700">Todos os seus dados financeiros serão removidos.</p><div class="mt-4 max-w-sm"><x-form.input label="Confirme sua senha" name="password" type="password" required /></div><x-button type="submit" variant="danger" class="mt-4">Excluir minha conta</x-button></form>
</x-app-layout>
